extends main.pug

block content
    div(class="bgc-white bd bdrs-3 p-20 mB-20")
        h4 #{localization.calculation}
        hr(class="divider")
        each val in details         
            div(class="form-group col-md-6")
                label #{val.name} - #{val.addedAmount}